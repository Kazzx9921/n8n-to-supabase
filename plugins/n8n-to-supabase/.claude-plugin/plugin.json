{
  "name": "n8n-to-supabase",
  "description": "Migrate n8n workflows to Supabase Edge Functions, PostgreSQL tables, secrets, and pg_cron schedules. Parses n8n JSON, generates migration plans, creates tables, writes Edge Functions, sets up secrets and cron jobs.",
  "version": "1.0.0"
}
